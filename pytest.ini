[pytest]
testpaths = tests
markers =
    smoke: fast critical-path checks; parametrized tests may produce multiple items
    regression: complete PhoneBook regression suite, including negative scenarios